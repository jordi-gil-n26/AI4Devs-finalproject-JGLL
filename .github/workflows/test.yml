name: Tests

on:
  pull_request:
  push:
    branches: [main]

jobs:
  backend:
    name: Backend (Gradle)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('backend/**/*.gradle*', 'backend/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: gradle-${{ runner.os }}-

      # --rerun-tasks defeats task caching so tests always execute (never
      # reported as up-to-date), and the test report is uploaded for inspection.
      - name: Run tests
        run: ./gradlew test --no-daemon --rerun-tasks

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-results
          path: backend/build/test-results/test/*.xml
          if-no-files-found: warn

  frontend:
    name: Frontend (Vitest)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          # Enables caching for faster builds and prevents corrupt cache errors
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      # Fixes the "npm error Exit handler never called!" CLI bug
      - name: Upgrade npm to latest stable
        run: npm install -g npm@latest

      - name: Install dependencies
        run: npm ci

      # --if-present is a no-op until the test runner lands (T020, #105),
      # so this job stays green on branches that predate it.
      - name: Run tests
        run: npm run test --if-present
