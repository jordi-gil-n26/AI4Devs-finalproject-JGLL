# Git worktrees created for issue/feature isolation — never commit these.
# They are local checkouts (and were previously committed as stray gitlinks).
/.worktrees/
/.claude/worktrees/

# Local evaluation — not for version control
/evaluation.md
